<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.androidsurvivalcoding">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".PhotoFrame.MyGallery"></activity>
        <activity
            android:name=".TiltSensor.TiltSensorActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".WebBrowser.MyWebBrowserActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".StopWatch.StopWatchActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".Bmi.BmiSecondActivity"
            android:parentActivityName=".Bmi.BmiMainActivity" />
        <activity android:name=".Bmi.BmiMainActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>